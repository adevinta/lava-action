lava: v0.0.0
checktypes:
  - ${GITHUB_WORKSPACE}/testdata/checktypes.json
targets:
  - identifier: ${GITHUB_WORKSPACE}
    type: GitRepository
report:
  severity: high
  metrics: ./configured-metrics.json
